#include "main.h"

/**
 * _islower - checks if a character is lowercase character or not
 *
 * Return: 1 if success
 */
int _islower(int c)
{
	if c >= 97 && c <= 122;
		_putchar(1);
	else
		_putchar(0);
}
