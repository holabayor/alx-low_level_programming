CC = gcc
SRC = main.c school.c
OBJ = $(patsubst %.c,%.o,$(SRC))
NAME = school

all : $(OBJ)
	$(CC) $^ -o $(NAME)

%.o : %.c
	$(CC) -c -o $@ $^

clean :
	rm -f $(NAME)

oclean :
	rm *.o

fclean:
	rm -f *.o $(NAME)